{% extends 'base.html.twig' %}

{% block title %}Contact{% endblock %}

{% form_theme contact_form 'bootstrap_5_layout.html.twig' %}


{% block body %}

        <h1 class="text-center"> Contactez-nous ! </h1>

        {# <h3>The contact has the following errors</h3>
        <ul>
            {% for error in errors %}
                <li>{{ error.message }}</li>
            {% endfor %}
        </ul> #}

        {# Afficher les msg flash#}
        {% for msg in app.flashes('confirmation') %}
            <div class="alert alert-success">
                {{ msg }}
            </div>
        {% endfor %}
    
        {{ form(contact_form, {attr: {'novalidate': 'novalidate', 'class': 'row col-md-8 mx-auto mt-3'}}) }}

{% endblock %}
